<h1>Stops</h1>
<table>
  <thead>
    <th>Stop id</th>
    <th>Routes</th>
    <th>On street</th>
    <th>Cross street</th>
    <th>Boardings</th>
    <th>Alightings</th>
  </thead>
  <tbody>
    <% @stops.each do |s| %>
      <tr>
        <td><%= link_to(s.stop_id, stop_path(s)) %></td>
        <td><%= s.route_list %></td>
        <td><%= s.street_on %></td>
        <td><%= s.street_cross %></td>
        <td><%= s.boardings %></td>
        <td><%= s.alightings %></td>
      </tr> 
    <% end %>
  </tbody>
</table>